# 작업 기록일지
## ──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
## 2026-07-07
# 개선 필요 
🔴 지금 당장 위험 — 정확성에 직결
1. 인덱스 정합성 (벡터 ↔ 텍스트 순서)
증분 인덱싱에서 "FAISS 벡터 순서 = parquet 행 순서"를 전제로 병합합니다 (ingest_flow.py). 이 가정이 한 번이라도 어긋나면 벡터는 A인데 텍스트는 B가 나오는 조용한 오류가 생깁니다. chunk_id 기반 매핑으로 바꿔야 근본적으로 안전합니다.

2. 테스트 부재
지금까지 우리가 고친 검색 버그들(커버리지, 극단값 등)을 지켜줄 자동 테스트가 없습니다. retrieve() 회귀 테스트만 있어도 다음 수정 때 안심입니다.

🟡 범용성을 막는 부분 — 사용자가 중요하게 본 지점
3. 문서 유형 자동 분류 (예전에 직접 언급하신 것)
parse.py가 아직 파일명에 "receipt"가 있는지로 분기합니다. 내용 기반 분류로 바꿔야 어떤 문서든 대응됩니다.

4. 하드코딩된 답변 추출 레이어
api.py의 make_answer에 대출한도, 배당기준일 같은 도메인별 정규식이 잔뜩 박혀 있습니다. 지금은 LLM이 죽었을 때만 쓰는 fallback인데, 특정 문서 전용이라 범용성과 충돌합니다. 정리 여부를 결정해야 합니다.

5. 구조 인식 청킹
3절 21조가 검색 어려웠던 근본 원인은 400자 고정 청킹이 조/절 경계를 잘라서였습니다. 조·절 단위를 존중하는 청킹이면 법령·규정 문서 전반이 좋아집니다.

🟢 운영 완성도 — CI/CD로 가려면
6. 배포 준비 — requirements 고정, Dockerfile, --reload 끈 운영 설정, 환경변수 정리
7. 관측성 — 검색 점수 로깅, "답변 못 찾음" 비율 추적 (UI의 max 0.0640 하드코딩도 이 맥락의 흔적)
##
# 작업 내용
1. E:\ragToy\data\raw 에 파일 업로드 시 자동 인덱싱
  new터미널
    venv\Scripts\activate
      python -m app.watcher 실행 필요

2. 인덱싱 되는동안 streamlit ui에 인덱싱 표시
3. 답변 품질 향상
    키워드 커버리지 가산점 추가
      - 질문 핵심 키워드를 2개 이상 동시에 포함한 청크에만 커버리지 비율 × 0.05 가산
      - 단일 키워드 질문(대부분의 eval 질문)은 covered < 2라 가산 없음 → 기존 동작 그대로
      - 파일명·도메인·질문 유형에 의존하지 않는 순수 IR 원리
      - top_k: 3 > 5

4. 답변 스트리밍 (체감 속도 개선)
   - 답변을 다 만들 때까지 기다렸다가 통째로 보여주던 방식을, 토큰을 생성되는 대로 한 글자씩 실시간 표시하도록 바꿨습니다.

5. UI 진행 상태 표시
   - 답변 말풍선에 🔍 문서 검색 중... → ✍️ 답변 작성 중... 단계별 상태 배지를 추가해 진행 상황이 보이게 했습니다.

6. 콜드 스타트 제거 (서버 워밍업)
   - 서버 시작 시 임베딩 모델을 미리 로딩해서, 첫 질문이 느려지는 문제를 없앴습니다.
      식당에 비유하면, 손님 오기 전에 미리 화구를 예열해두는 것과 같습니다. 주문 받고 그때부터 불 켜면 첫 손님이 오래 기다리니까, 영업 시작할 때 미리 켜두는 거죠.
## ──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────